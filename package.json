{
  "name": "pac-man-royal",
  "version": "1.0.0",
  "private": true,
  "description": "Pac-Royale — 1 Pac-Royale vs 4 ghosts. Capture to become Pac-Royale. Highest score wins.",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npx tsx watch server/src/index.ts",
    "postinstall": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npx tsx server/src/index.ts",
    "preview": "npm run preview --prefix client",
    "build:apk": "npm run build:apk --prefix android-app"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
